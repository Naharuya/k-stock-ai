import assert from 'node:assert/strict';
import {assess} from './operations_morning_check.mjs';
const date='2026-09-14',at='2026-09-13T22:00:00.000Z',names=['MarketAgent','NewsAgent','DARTAgent','ScreenerAgent','QuoteValidationAgent','FundamentalAgent','ValuationAgent','TechnicalAgent','FlowAgent','RiskAgent','BearAgent','InvestmentCommittee','EntryAgent','ExitAgent'];
const fixture={date,now:new Date('2026-09-13T22:10:00Z'),health:{ok:true,version:'2.6.4',liveTrading:false},report:{date,scope:'all',engineVersion:'2.6.4',runId:at,startedAt:at,completedAt:'2026-09-13T22:02:00Z',status:'SUCCESS',automaticOrders:false,tradingCalendarStatus:'READY',topCandidates:[],portfolioExitStatus:[],agentStatus:Object.fromEntries(names.map(n=>[n,{status:'SUCCESS'}]))},scheduler:{lastAttemptAt:at,runId:at,status:'SUCCESS',attempts:1},logCoverage:true};
assert.equal(assess(fixture).verdict,'PASS');
for(const [change,want]of [[x=>x.now=new Date('2026-09-13T12:00:00Z'),'PENDING'],[x=>x.report=null,'PENDING'],[x=>x.report.tradingCalendarStatus='FALLBACK','PARTIAL'],[x=>x.health.liveTrading=true,'FAIL'],[x=>x.report.runId='manual','FAIL'],[x=>x.report.scope='portfolio','FAIL'],[x=>x.scheduler.lastAttemptAt='2026-09-13T23:00:00Z','FAIL'],[x=>x.report.agentStatus.NewsAgent.status='FAILED','PARTIAL'],[x=>x.events=[{at,event:'UNCAUGHT_EXCEPTION'}],'FAIL'],[x=>x.scheduler.attempts=2,'PARTIAL']]){const x=structuredClone(fixture);change(x);assert.equal(assess(x).verdict,want);}
const missing=structuredClone(fixture);missing.report=null;missing.now=new Date('2026-09-13T23:00:00Z');assert.equal(assess(missing).verdict,'FAIL');
console.log('Morning checker PASS: due time, run identity, fallback, agent failures, missing report, crashes, live safety, ambiguous retries; no analysis invoked.');
